import { MapHostDirective } from './map-host.directive';

describe('MapHostDirective', () => {
  it('should create an instance', () => {
    const directive = new MapHostDirective();
    expect(directive).toBeTruthy();
  });
});
